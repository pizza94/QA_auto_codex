# DQI 관리 자동화 실행 결과

실행일: 2026-07-03
대상 메뉴: `검증기준관리 > 데이터품질지표(DQI)관리`
대상 URL: `/quality_woori/main#dqi`

## 테스트 데이터

- 대분류: `AUTO_DQI_BIG_20260703020936`
- 소분류 최초값: `AUTO_DQI_SUB_20260703020936`
- 소분류 수정값: `AUTO_DQI_SUB_20260703020936_EDIT`
- 목표수준: `95 -> 96`

## 검증 결과

| 항목 | 결과 | 비고 |
| --- | --- | --- |
| DQI 메뉴 진입 | 통과 | `QualityStream 4.3`, `#dqiRegion` 확인 |
| 필수값 빨간 별표 | 통과 | `::after` content `*`, color `rgb(217, 45, 32)` 확인 |
| 필수값 미입력 저장 차단 | 통과 | `필수 입력입니다.` 메시지 표시 |
| 상단 신규로 대분류 생성 | 통과 | 트리에 대분류 생성 및 상세값 일치 |
| 대분류 우클릭 신규로 소분류 생성 | 통과 | 컨텍스트 메뉴 `.vakata-context` 사용 |
| 소분류 수정 후 저장 | 통과 | 저장 성공 알림 확인 |
| 수정값 재조회 | 통과 | 재진입 후 수정값 유지 |
| Search 완전일치/부분일치 | 통과 | 생성 데이터 기준 트리 필터링 확인 |
| Search 미존재값 | 확인 필요 | 빈 결과가 아니라 전체 트리가 다시 보이는 동작 확인 |
| BR상세정보 | 조건부 통과 | BR 행 미선택 상태에서 `연관 BR정보를 선택해 주세요.` 알림 표시 |
| 연관BR 리포트 | 조건부 통과 | 관련 BR 결과 없음 상태에서 `출력할 검색 결과가 없습니다.` 알림 표시 |
| 소분류 삭제 | 통과 | 삭제 완료 후 재조회 시 미노출 |
| 대분류 삭제 | 통과 | 삭제 완료 후 재조회 시 미노출 |

## 주요 셀렉터

| 대상 | 셀렉터 |
| --- | --- |
| 신규 | `#newNodeButton` |
| 저장 | `#dqiSaveButton` |
| 트리 검색 | `#dqiTree #tableTreeSearch` |
| 모두열기/모두닫기 | `#allOpenButton` |
| 대분류명 | `[name="dqiName"]` |
| 소분류명 | `[name="subDqiName"]` |
| 대분류 설명 | `[name="dqiDesc"]` |
| 소분류 설명 | `[name="subDqiDesc"]` |
| 목표수준 | `[name="desiredQuality"]` |
| BR상세정보 | `#brDetailInfo` |
| 연관BR 리포트 | `#brReport` |
| 트리 우클릭 메뉴 | `.vakata-context` |

## 자동화 반영

- 테스트 파일: `tests/dqi-management.spec.ts`
- 실행 명령: `npm run test:dqi`

## 참고사항

1. 소분류 생성은 상단 `신규`가 아니라 대분류 노드 우클릭 후 컨텍스트 메뉴의 `신규`로 수행한다.
2. 상단 더보기 영역의 DQI 리포트는 화면에 항목이 표시되지만 수동 MCP 실행 중 다운로드 이벤트가 확인되지 않았다. 현재 자동화에는 연관BR 리포트의 빈 결과 안내 검증을 반영했다.
3. 테스트 데이터는 생성 후 소분류, 대분류 순서로 삭제했다. 최종 재조회에서 두 데이터 모두 미노출을 확인했다.
